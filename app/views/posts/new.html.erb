<%= current_bloguer.id %>
<%= form_for @post, url: {action: "create"}, html: {class: "nifty_form"} do |f| %>
    <%= f.text_field :title %>
    <%= f.text_area :body, :class => "froala-editor" %>
    <%= f.submit "Create" %>
<% end %>

